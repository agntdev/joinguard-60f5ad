[
  {
    "name": "Verify",
    "steps": [
      {
        "expect": [
          {
            "method": "editMessageText",
            "payload": { "text": "New members receive a code when they join. If you have a pending code, send it here." }
          }
        ],
        "send": {
          "callback": "verify:start"
        }
      }
    ]
  }
]
